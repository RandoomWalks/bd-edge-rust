[package]
name = "bench"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true

# For load testing / benchmarking
tokio-util = { version = "0.7", features = ["full"] }

